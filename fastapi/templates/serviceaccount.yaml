# helm/fastapi/templates/serviceaccount.yaml
{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "fastapi.serviceAccountName" . }}
  labels:
    {{- include "fastapi.labels" . | nindent 4 }}
{{- end }}